name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - master
      - dev

jobs:
  build-and-test:
    uses: Sentience-Robotics/.github/.github/workflows/robot-package-ci.yaml@master
    with:
      package_name: inmoov_urdf
    secrets: inherit
